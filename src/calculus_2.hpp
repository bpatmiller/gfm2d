#pragma once
#include <glm/glm.hpp>

/** returns the central differenced divergence */
float divergence(Array2f& field, vec2 world_position) {
    return 0;
}

/** returns the central differenced gradient of a point in space, */
vec2 gradient(Array2f& field, vec2 world_position) {
    return vec2(0);
}

